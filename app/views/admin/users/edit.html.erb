<div class='page-header'>
  <h3><i class='icon-play-circle'> </i>Editing user</h3>
</div>
<%= simple_form_for [:admin,@user] do |f| %>
  <div class='container'>
    <%= f.input :fullname %>
    <%= f.association :group %>
  </div>
  <div class='form-actions'>
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
