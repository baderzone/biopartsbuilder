<h2><i class='icon-play-circle'> </i>Editing user</h2>
<hr />
<%= simple_form_for [:admin,@user] do |f| %>
  <%= f.input :fullname %>
  <%= f.association :group %>
  <div class='form-actions'>
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
